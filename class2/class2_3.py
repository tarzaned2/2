class Ezen1304:

    def __init__(self,roomnumber,roomcheck):
        #self 전역변수
        #python 초기값을 넣어야한다 아니면 에러
        #지역변수는 해당하는 문이 닫히면 소멸한다
        #지역변수가 소멸하기전에 전역변수에 담는다
        self.roomnumber=roomnumber
        self.roomcheck=roomcheck
    def printe(self):
        print(self.roomnumber)
        print(self.roomcheck)

e1304=Ezen1304('room1304',6)
#object 모든 타입을 담을 수 있다
#부모 배스킨라빈스아이스크림   (31), 다른 아이스크림 ->아이스크림 팩토리 31가지 맛 ->대리점(판매)
#interface뼈대 commandice  객체생성할수없다
#명령 31 메소드 +다른 아이스크림 (20) (몸체)
#상속 아이스크림 공장-> 상속받는 몸체구현을 한다 안하면 에러
#상속 대리점 (100)
#엄마는외계인 아빠는지구인 '

#현재 객체에서 현재 객체

#commandice.com =1대리점('엄마는외계인')

#commandice.com =2대리점('아빠는지구인')
# commandice(parent)=>commandfactory->1sell
from common import if_1


e1304.

